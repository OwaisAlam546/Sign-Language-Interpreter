fastapi>=0.115,<1
uvicorn[standard]>=0.30
numpy>=1.26
python-dotenv>=1.0
httpx>=0.27            # TestClient transport for scripts/smoke_test.py

# ── optional, feature-gated at runtime ──────────────────────────
# opencv-python-headless   # real camera capture (CameraManager)
# gTTS                     # real neural voices for /text-to-speech
# tensorflow               # load trained .h5/.keras LSTM weights (Phase 8)