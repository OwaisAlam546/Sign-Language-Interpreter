{
  "name": "signspeak-ai",
  "version": "0.1.0",
  "private": true,
  "description": "SignSpeak AI inference microservice (FastAPI) — model, camera, prediction, TTS",
  "scripts": {
    "test": "node scripts/run_smoke.js",
    "verify": "node scripts/run_smoke.js",
    "start": "\".venv\\Scripts\\python.exe\" -m uvicorn app.main:app --port 8000"
  }
}